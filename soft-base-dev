#normal

sudo yum install -y  

git gcc gdb gcc-c++ 
vim telnet wget curl net-tools tree unzip zip nmap

mysql-community-common

#fedora

#centos
 
#something:

sqlmap 

go openssh-server android-tools cmake nodejs npm bnpm cnpm dockers redis mongodb nmap sqlmap virsh
clang
java mvn 
pip install --user sqlmap

npm install vue webpack

sudo cnpm install --global vue-cli

# php golang

sudo dnf install php golang
# 依赖httpd



# more
netease-cloud-music vlc 


# cmd

netstat
